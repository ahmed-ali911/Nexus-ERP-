"""Audit trail engine — placeholder.

Future home for: change-tracking (who/what/when/old-value/new-value) across
modules, likely as a SQLAlchemy event listener or decorator applied per
model. Intentionally empty until the first module needs it.
"""
