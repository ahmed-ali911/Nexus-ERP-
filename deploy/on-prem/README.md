# On-Prem Deployment

Placeholder for on-premise deployment assets (client server install scripts,
systemd/service units, backup/restore scripts, local reverse-proxy config,
etc.). Empty until a client deployment target is defined.
